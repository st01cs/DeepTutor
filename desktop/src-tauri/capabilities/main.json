{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Core capability for the DeepTutor desktop window (splash + local UI).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
